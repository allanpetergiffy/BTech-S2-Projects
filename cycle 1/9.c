//    9. Assign the same value to multiple variables of the same type.
#include <stdio.h>
int main() {
printf(" 9. Assign the same value to multiple variables of the same type.\n");
 int x, y, z;
 x = y = z = 50;
 printf("%d", x + y + z);
 return 0;
}
